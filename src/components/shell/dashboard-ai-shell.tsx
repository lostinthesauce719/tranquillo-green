"use client";

import { type ReactNode } from "react";

// Placeholder: AI assistant removed per swarm consensus.
// Real AI integration planned. This wrapper is now a passthrough.
export function DashboardAiShell({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
